# 자료 정리와 커밋 검증 기록

2026-09-11. 사용자 요청으로 전체 준비 자료를 저장하며 문서와 이미지 용도를 정리했다.

- 문서 61개를 주제별 한글 폴더로 재배치하고 참조 파일 102개를 갱신했다. 기존에 유효한 Markdown 링크 1,304개를 보존했고, 보관 문서에 남아 있던 잘못된 링크 2개도 수정했다.
- `게임제작/`에 현재 보완 장면 기준 인게임 이미지 462개, 참고 합성 60개, 편집 원본 94개를 분리했다. 원본과 복사본 SHA-256을 분류표에 저장한다. 전체 Unity 자산을 확정한 것은 아니며 회상·UI 등의 나머지 후보는 추가 대조 대상이다.
- `art/` 제작 원본·이력과 `design/unity-handoff/opening-v1/` 고정 보관본은 경로를 유지한다. 실험 자료를 최종 이미지로 오인하지 않도록 각 폴더의 README에 용도를 구분했다.
- 탐색 V1/V2, 요리, 화덕 실험의 규칙 테스트 통과. 화덕은 미채택 상태를 유지한다.
- 화자 공개 19건, 시작 연출 38건, 회상 대화 42건, 사진 회상 24건 검증 통과. C0 이어가기 352개 경로 검증 통과.
- **기존 검사 미통과:** `tools/test-ch00-01-narrative-revision.mjs`는 정리 작업 전부터 `design/ui/ch00-01/memory-dialogue.json`의 과거 보호 해시가 현재 파일과 달라 실패했다. 예상 `373d9da3…`, 현재 `094a929e…`. 현재 동작 검증과 과거 고정 파일 검사 범위가 다르므로 통과한 것으로 보고하지 않는다. 해시를 임의 갱신하거나 현재 원화를 과거로 되돌리지 않았다. Unity 전환 시 해당 과거 검사 기준의 유효 범위를 확인할 필요가 있다.
- 이미지·PSD·폰트는 Git LFS로 관리한다. 저장소 전체의 원화 품질 검수를 이번 커밋 과정에서 새로 반복한 것은 아니다.

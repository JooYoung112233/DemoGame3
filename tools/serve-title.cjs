const http=require('node:http'),fs=require('node:fs'),path=require('node:path');
const root=path.resolve(__dirname,'..'),port=Number(process.argv[2]||8785);
const mime={'.png':'image/png','.svg':'image/svg+xml','.html':'text/html; charset=utf-8','.css':'text/css; charset=utf-8','.js':'text/javascript; charset=utf-8','.json':'application/json; charset=utf-8','.woff2':'font/woff2','.ttf':'font/ttf'};
http.createServer((req,res)=>{let url;try{url=decodeURIComponent(new URL(req.url,'http://localhost').pathname);}catch{res.writeHead(400);return res.end();}const file=path.resolve(root,'.'+(url==='/'?'/design/title/review.html':url));const allowed=['art/title','design/title'].some(dir=>file.startsWith(path.resolve(root,dir)+path.sep));if(!allowed){res.writeHead(403);return res.end();}fs.readFile(file,(err,data)=>{if(err){res.writeHead(404);return res.end('Not found');}res.setHeader('Content-Type',mime[path.extname(file)]||'text/plain; charset=utf-8');res.setHeader('Cache-Control','no-cache');res.end(data);});}).listen(port,'127.0.0.1',()=>console.log('http://127.0.0.1:'+port));
